services:
  - type: web
    name: drug-bot
    runtime: docker
    plan: free
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: DEBUG_GUILD
        sync: false
